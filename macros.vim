" Macros that work with my neovim setup
" To yank into a buffer <buffer>, put cursor at start of line and do
" "<buffer>Y
" Example yanking into buffer `r`
" "rY
" Then it can be applied with @r

" Switch conditional order `if (ret == 0) {` -> `if (0 == ret) {`
If(ldwf)i pa€kbIf(lvf dea pa€kD
